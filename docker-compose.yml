
networks:
  app:
services:
  backend:
    build: ./backend
    ports:
      - 8000:8000
    networks:
      - app
  frontend:
    build: ./frontend/childrenapp
    ports:
      - 3000:3000
    networks:
      - app
  nginx:
    image: nginx:stable-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - frontend
      - backend
    networks:
      - app
    
    
    